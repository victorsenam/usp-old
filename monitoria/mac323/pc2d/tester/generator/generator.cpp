#include <bits/stdc++.h>
using namespace std;

#include "../testlib.h"

int main (int argc, char ** argv) {
    registerGen(argc, argv, 1);
    int lo_n, hi_n;
    sscanf(argv[1], "%d", &lo);
    sscanf(argv[2], "%d", &hi);
    printf("%d %d\n", lo, hi);

    int n = rnd.next(lo, hi);

    double lo_d, hi_d;


}
